// Placeholder — vitest unit suite entry. Real unit tests land per package
// in packages/<pkg>/src/**/*.test.ts; this file just keeps the dir mounted.
import { describe, expect, it } from 'vitest'

describe('unit placeholder', () => {
  it('has a dir', () => {
    expect(1).toBe(1)
  })
})
