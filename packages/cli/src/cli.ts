/**
 * CLI implementation. Wires the pure ports with no DSH runtime so the full
 * lifecycle can be validated headlessly. Skeleton placeholder until the
 * core services' Phase 1 fills in.
 */

export async function runCli(argv: string[]): Promise<void> {
  void argv
  // Phase 1: commander entry moves here from bin/dsh-lab.js
}
