{
  "name": "@dsh-lab/core",
  "version": "0.1.0",
  "private": true,
  "description": "Business use-case orchestration for the Deep Learning Lab — Solution lifecycle, Run lifecycle, reconcile (no DSH dependency)",
  "type": "module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "files": ["lib"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@dsh-lab/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
