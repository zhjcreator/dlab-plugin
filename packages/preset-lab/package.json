{
  "name": "@dlab/preset-lab",
  "version": "0.1.0",
  "private": true,
  "description": "DSH agent preset that grants every session the lab_* tools + lab context",
  "type": "module",
  "files": ["preset.yml", "agent.cordis.yml", "skills"],
  "dsh": {
    "bundle": { "patch": "./agent.cordis.yml" }
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
