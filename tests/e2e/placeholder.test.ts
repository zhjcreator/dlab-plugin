// Placeholder — e2e suite (CLI + LabService) lands in Phase 1.
import { describe, expect, it } from 'vitest'

describe('e2e placeholder', () => {
  it('is a dir', () => {
    expect(1).toBe(1)
  })
})
